// Code generated by ent, DO NOT EDIT.

package ent

import (
	"api_server/ent/datasetroot"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// DatasetRoot is the model entity for the DatasetRoot schema.
type DatasetRoot struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Path holds the value of the "path" field.
	Path string `json:"path,omitempty"`
	// IsUse holds the value of the "is_use" field.
	IsUse bool `json:"is_use,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DatasetRootQuery when eager-loading is set.
	Edges        DatasetRootEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DatasetRootEdges holds the relations/edges for other nodes in the graph.
type DatasetRootEdges struct {
	// Datasets holds the value of the datasets edge.
	Datasets []*Dataset `json:"datasets,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DatasetsOrErr returns the Datasets value or an error if the edge
// was not loaded in eager-loading.
func (e DatasetRootEdges) DatasetsOrErr() ([]*Dataset, error) {
	if e.loadedTypes[0] {
		return e.Datasets, nil
	}
	return nil, &NotLoadedError{edge: "datasets"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DatasetRoot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case datasetroot.FieldIsUse:
			values[i] = new(sql.NullBool)
		case datasetroot.FieldID:
			values[i] = new(sql.NullInt64)
		case datasetroot.FieldName, datasetroot.FieldPath:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DatasetRoot fields.
func (dr *DatasetRoot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case datasetroot.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			dr.ID = int(value.Int64)
		case datasetroot.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				dr.Name = value.String
			}
		case datasetroot.FieldPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field path", values[i])
			} else if value.Valid {
				dr.Path = value.String
			}
		case datasetroot.FieldIsUse:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_use", values[i])
			} else if value.Valid {
				dr.IsUse = value.Bool
			}
		default:
			dr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DatasetRoot.
// This includes values selected through modifiers, order, etc.
func (dr *DatasetRoot) Value(name string) (ent.Value, error) {
	return dr.selectValues.Get(name)
}

// QueryDatasets queries the "datasets" edge of the DatasetRoot entity.
func (dr *DatasetRoot) QueryDatasets() *DatasetQuery {
	return NewDatasetRootClient(dr.config).QueryDatasets(dr)
}

// Update returns a builder for updating this DatasetRoot.
// Note that you need to call DatasetRoot.Unwrap() before calling this method if this DatasetRoot
// was returned from a transaction, and the transaction was committed or rolled back.
func (dr *DatasetRoot) Update() *DatasetRootUpdateOne {
	return NewDatasetRootClient(dr.config).UpdateOne(dr)
}

// Unwrap unwraps the DatasetRoot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dr *DatasetRoot) Unwrap() *DatasetRoot {
	_tx, ok := dr.config.driver.(*txDriver)
	if !ok {
		panic("ent: DatasetRoot is not a transactional entity")
	}
	dr.config.driver = _tx.drv
	return dr
}

// String implements the fmt.Stringer.
func (dr *DatasetRoot) String() string {
	var builder strings.Builder
	builder.WriteString("DatasetRoot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dr.ID))
	builder.WriteString("name=")
	builder.WriteString(dr.Name)
	builder.WriteString(", ")
	builder.WriteString("path=")
	builder.WriteString(dr.Path)
	builder.WriteString(", ")
	builder.WriteString("is_use=")
	builder.WriteString(fmt.Sprintf("%v", dr.IsUse))
	builder.WriteByte(')')
	return builder.String()
}

// DatasetRoots is a parsable slice of DatasetRoot.
type DatasetRoots []*DatasetRoot
