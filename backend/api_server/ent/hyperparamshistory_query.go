// Code generated by ent, DO NOT EDIT.

package ent

import (
	"api_server/ent/hyperparamshistory"
	"api_server/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// HyperParamsHistoryQuery is the builder for querying HyperParamsHistory entities.
type HyperParamsHistoryQuery struct {
	config
	ctx        *QueryContext
	order      []hyperparamshistory.OrderOption
	inters     []Interceptor
	predicates []predicate.HyperParamsHistory
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HyperParamsHistoryQuery builder.
func (hphq *HyperParamsHistoryQuery) Where(ps ...predicate.HyperParamsHistory) *HyperParamsHistoryQuery {
	hphq.predicates = append(hphq.predicates, ps...)
	return hphq
}

// Limit the number of records to be returned by this query.
func (hphq *HyperParamsHistoryQuery) Limit(limit int) *HyperParamsHistoryQuery {
	hphq.ctx.Limit = &limit
	return hphq
}

// Offset to start from.
func (hphq *HyperParamsHistoryQuery) Offset(offset int) *HyperParamsHistoryQuery {
	hphq.ctx.Offset = &offset
	return hphq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hphq *HyperParamsHistoryQuery) Unique(unique bool) *HyperParamsHistoryQuery {
	hphq.ctx.Unique = &unique
	return hphq
}

// Order specifies how the records should be ordered.
func (hphq *HyperParamsHistoryQuery) Order(o ...hyperparamshistory.OrderOption) *HyperParamsHistoryQuery {
	hphq.order = append(hphq.order, o...)
	return hphq
}

// First returns the first HyperParamsHistory entity from the query.
// Returns a *NotFoundError when no HyperParamsHistory was found.
func (hphq *HyperParamsHistoryQuery) First(ctx context.Context) (*HyperParamsHistory, error) {
	nodes, err := hphq.Limit(1).All(setContextOp(ctx, hphq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hyperparamshistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hphq *HyperParamsHistoryQuery) FirstX(ctx context.Context) *HyperParamsHistory {
	node, err := hphq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HyperParamsHistory ID from the query.
// Returns a *NotFoundError when no HyperParamsHistory ID was found.
func (hphq *HyperParamsHistoryQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hphq.Limit(1).IDs(setContextOp(ctx, hphq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hyperparamshistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hphq *HyperParamsHistoryQuery) FirstIDX(ctx context.Context) int {
	id, err := hphq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HyperParamsHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HyperParamsHistory entity is found.
// Returns a *NotFoundError when no HyperParamsHistory entities are found.
func (hphq *HyperParamsHistoryQuery) Only(ctx context.Context) (*HyperParamsHistory, error) {
	nodes, err := hphq.Limit(2).All(setContextOp(ctx, hphq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hyperparamshistory.Label}
	default:
		return nil, &NotSingularError{hyperparamshistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hphq *HyperParamsHistoryQuery) OnlyX(ctx context.Context) *HyperParamsHistory {
	node, err := hphq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HyperParamsHistory ID in the query.
// Returns a *NotSingularError when more than one HyperParamsHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (hphq *HyperParamsHistoryQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hphq.Limit(2).IDs(setContextOp(ctx, hphq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hyperparamshistory.Label}
	default:
		err = &NotSingularError{hyperparamshistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hphq *HyperParamsHistoryQuery) OnlyIDX(ctx context.Context) int {
	id, err := hphq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HyperParamsHistories.
func (hphq *HyperParamsHistoryQuery) All(ctx context.Context) ([]*HyperParamsHistory, error) {
	ctx = setContextOp(ctx, hphq.ctx, ent.OpQueryAll)
	if err := hphq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HyperParamsHistory, *HyperParamsHistoryQuery]()
	return withInterceptors[[]*HyperParamsHistory](ctx, hphq, qr, hphq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hphq *HyperParamsHistoryQuery) AllX(ctx context.Context) []*HyperParamsHistory {
	nodes, err := hphq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HyperParamsHistory IDs.
func (hphq *HyperParamsHistoryQuery) IDs(ctx context.Context) (ids []int, err error) {
	if hphq.ctx.Unique == nil && hphq.path != nil {
		hphq.Unique(true)
	}
	ctx = setContextOp(ctx, hphq.ctx, ent.OpQueryIDs)
	if err = hphq.Select(hyperparamshistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hphq *HyperParamsHistoryQuery) IDsX(ctx context.Context) []int {
	ids, err := hphq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hphq *HyperParamsHistoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hphq.ctx, ent.OpQueryCount)
	if err := hphq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hphq, querierCount[*HyperParamsHistoryQuery](), hphq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hphq *HyperParamsHistoryQuery) CountX(ctx context.Context) int {
	count, err := hphq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hphq *HyperParamsHistoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hphq.ctx, ent.OpQueryExist)
	switch _, err := hphq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hphq *HyperParamsHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := hphq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HyperParamsHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hphq *HyperParamsHistoryQuery) Clone() *HyperParamsHistoryQuery {
	if hphq == nil {
		return nil
	}
	return &HyperParamsHistoryQuery{
		config:     hphq.config,
		ctx:        hphq.ctx.Clone(),
		order:      append([]hyperparamshistory.OrderOption{}, hphq.order...),
		inters:     append([]Interceptor{}, hphq.inters...),
		predicates: append([]predicate.HyperParamsHistory{}, hphq.predicates...),
		// clone intermediate query.
		sql:       hphq.sql.Clone(),
		path:      hphq.path,
		modifiers: append([]func(*sql.Selector){}, hphq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TrialID int `json:"trial_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HyperParamsHistory.Query().
//		GroupBy(hyperparamshistory.FieldTrialID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hphq *HyperParamsHistoryQuery) GroupBy(field string, fields ...string) *HyperParamsHistoryGroupBy {
	hphq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HyperParamsHistoryGroupBy{build: hphq}
	grbuild.flds = &hphq.ctx.Fields
	grbuild.label = hyperparamshistory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TrialID int `json:"trial_id,omitempty"`
//	}
//
//	client.HyperParamsHistory.Query().
//		Select(hyperparamshistory.FieldTrialID).
//		Scan(ctx, &v)
func (hphq *HyperParamsHistoryQuery) Select(fields ...string) *HyperParamsHistorySelect {
	hphq.ctx.Fields = append(hphq.ctx.Fields, fields...)
	sbuild := &HyperParamsHistorySelect{HyperParamsHistoryQuery: hphq}
	sbuild.label = hyperparamshistory.Label
	sbuild.flds, sbuild.scan = &hphq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HyperParamsHistorySelect configured with the given aggregations.
func (hphq *HyperParamsHistoryQuery) Aggregate(fns ...AggregateFunc) *HyperParamsHistorySelect {
	return hphq.Select().Aggregate(fns...)
}

func (hphq *HyperParamsHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hphq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hphq); err != nil {
				return err
			}
		}
	}
	for _, f := range hphq.ctx.Fields {
		if !hyperparamshistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hphq.path != nil {
		prev, err := hphq.path(ctx)
		if err != nil {
			return err
		}
		hphq.sql = prev
	}
	return nil
}

func (hphq *HyperParamsHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HyperParamsHistory, error) {
	var (
		nodes = []*HyperParamsHistory{}
		_spec = hphq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HyperParamsHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HyperParamsHistory{config: hphq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(hphq.modifiers) > 0 {
		_spec.Modifiers = hphq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hphq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (hphq *HyperParamsHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hphq.querySpec()
	if len(hphq.modifiers) > 0 {
		_spec.Modifiers = hphq.modifiers
	}
	_spec.Node.Columns = hphq.ctx.Fields
	if len(hphq.ctx.Fields) > 0 {
		_spec.Unique = hphq.ctx.Unique != nil && *hphq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hphq.driver, _spec)
}

func (hphq *HyperParamsHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(hyperparamshistory.Table, hyperparamshistory.Columns, sqlgraph.NewFieldSpec(hyperparamshistory.FieldID, field.TypeInt))
	_spec.From = hphq.sql
	if unique := hphq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hphq.path != nil {
		_spec.Unique = true
	}
	if fields := hphq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hyperparamshistory.FieldID)
		for i := range fields {
			if fields[i] != hyperparamshistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := hphq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hphq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hphq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hphq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hphq *HyperParamsHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hphq.driver.Dialect())
	t1 := builder.Table(hyperparamshistory.Table)
	columns := hphq.ctx.Fields
	if len(columns) == 0 {
		columns = hyperparamshistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hphq.sql != nil {
		selector = hphq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hphq.ctx.Unique != nil && *hphq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range hphq.modifiers {
		m(selector)
	}
	for _, p := range hphq.predicates {
		p(selector)
	}
	for _, p := range hphq.order {
		p(selector)
	}
	if offset := hphq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hphq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (hphq *HyperParamsHistoryQuery) Modify(modifiers ...func(s *sql.Selector)) *HyperParamsHistorySelect {
	hphq.modifiers = append(hphq.modifiers, modifiers...)
	return hphq.Select()
}

// HyperParamsHistoryGroupBy is the group-by builder for HyperParamsHistory entities.
type HyperParamsHistoryGroupBy struct {
	selector
	build *HyperParamsHistoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hphgb *HyperParamsHistoryGroupBy) Aggregate(fns ...AggregateFunc) *HyperParamsHistoryGroupBy {
	hphgb.fns = append(hphgb.fns, fns...)
	return hphgb
}

// Scan applies the selector query and scans the result into the given value.
func (hphgb *HyperParamsHistoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hphgb.build.ctx, ent.OpQueryGroupBy)
	if err := hphgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HyperParamsHistoryQuery, *HyperParamsHistoryGroupBy](ctx, hphgb.build, hphgb, hphgb.build.inters, v)
}

func (hphgb *HyperParamsHistoryGroupBy) sqlScan(ctx context.Context, root *HyperParamsHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hphgb.fns))
	for _, fn := range hphgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hphgb.flds)+len(hphgb.fns))
		for _, f := range *hphgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hphgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hphgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HyperParamsHistorySelect is the builder for selecting fields of HyperParamsHistory entities.
type HyperParamsHistorySelect struct {
	*HyperParamsHistoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hphs *HyperParamsHistorySelect) Aggregate(fns ...AggregateFunc) *HyperParamsHistorySelect {
	hphs.fns = append(hphs.fns, fns...)
	return hphs
}

// Scan applies the selector query and scans the result into the given value.
func (hphs *HyperParamsHistorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hphs.ctx, ent.OpQuerySelect)
	if err := hphs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HyperParamsHistoryQuery, *HyperParamsHistorySelect](ctx, hphs.HyperParamsHistoryQuery, hphs, hphs.inters, v)
}

func (hphs *HyperParamsHistorySelect) sqlScan(ctx context.Context, root *HyperParamsHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hphs.fns))
	for _, fn := range hphs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hphs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hphs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (hphs *HyperParamsHistorySelect) Modify(modifiers ...func(s *sql.Selector)) *HyperParamsHistorySelect {
	hphs.modifiers = append(hphs.modifiers, modifiers...)
	return hphs
}
