// Code generated by ent, DO NOT EDIT.

package ent

import (
	"api_server/ent/enginelog"
	"api_server/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// EngineLogQuery is the builder for querying EngineLog entities.
type EngineLogQuery struct {
	config
	ctx        *QueryContext
	order      []enginelog.OrderOption
	inters     []Interceptor
	predicates []predicate.EngineLog
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EngineLogQuery builder.
func (elq *EngineLogQuery) Where(ps ...predicate.EngineLog) *EngineLogQuery {
	elq.predicates = append(elq.predicates, ps...)
	return elq
}

// Limit the number of records to be returned by this query.
func (elq *EngineLogQuery) Limit(limit int) *EngineLogQuery {
	elq.ctx.Limit = &limit
	return elq
}

// Offset to start from.
func (elq *EngineLogQuery) Offset(offset int) *EngineLogQuery {
	elq.ctx.Offset = &offset
	return elq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (elq *EngineLogQuery) Unique(unique bool) *EngineLogQuery {
	elq.ctx.Unique = &unique
	return elq
}

// Order specifies how the records should be ordered.
func (elq *EngineLogQuery) Order(o ...enginelog.OrderOption) *EngineLogQuery {
	elq.order = append(elq.order, o...)
	return elq
}

// First returns the first EngineLog entity from the query.
// Returns a *NotFoundError when no EngineLog was found.
func (elq *EngineLogQuery) First(ctx context.Context) (*EngineLog, error) {
	nodes, err := elq.Limit(1).All(setContextOp(ctx, elq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{enginelog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (elq *EngineLogQuery) FirstX(ctx context.Context) *EngineLog {
	node, err := elq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EngineLog ID from the query.
// Returns a *NotFoundError when no EngineLog ID was found.
func (elq *EngineLogQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = elq.Limit(1).IDs(setContextOp(ctx, elq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{enginelog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (elq *EngineLogQuery) FirstIDX(ctx context.Context) int {
	id, err := elq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EngineLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EngineLog entity is found.
// Returns a *NotFoundError when no EngineLog entities are found.
func (elq *EngineLogQuery) Only(ctx context.Context) (*EngineLog, error) {
	nodes, err := elq.Limit(2).All(setContextOp(ctx, elq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{enginelog.Label}
	default:
		return nil, &NotSingularError{enginelog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (elq *EngineLogQuery) OnlyX(ctx context.Context) *EngineLog {
	node, err := elq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EngineLog ID in the query.
// Returns a *NotSingularError when more than one EngineLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (elq *EngineLogQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = elq.Limit(2).IDs(setContextOp(ctx, elq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{enginelog.Label}
	default:
		err = &NotSingularError{enginelog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (elq *EngineLogQuery) OnlyIDX(ctx context.Context) int {
	id, err := elq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EngineLogs.
func (elq *EngineLogQuery) All(ctx context.Context) ([]*EngineLog, error) {
	ctx = setContextOp(ctx, elq.ctx, ent.OpQueryAll)
	if err := elq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EngineLog, *EngineLogQuery]()
	return withInterceptors[[]*EngineLog](ctx, elq, qr, elq.inters)
}

// AllX is like All, but panics if an error occurs.
func (elq *EngineLogQuery) AllX(ctx context.Context) []*EngineLog {
	nodes, err := elq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EngineLog IDs.
func (elq *EngineLogQuery) IDs(ctx context.Context) (ids []int, err error) {
	if elq.ctx.Unique == nil && elq.path != nil {
		elq.Unique(true)
	}
	ctx = setContextOp(ctx, elq.ctx, ent.OpQueryIDs)
	if err = elq.Select(enginelog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (elq *EngineLogQuery) IDsX(ctx context.Context) []int {
	ids, err := elq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (elq *EngineLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, elq.ctx, ent.OpQueryCount)
	if err := elq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, elq, querierCount[*EngineLogQuery](), elq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (elq *EngineLogQuery) CountX(ctx context.Context) int {
	count, err := elq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (elq *EngineLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, elq.ctx, ent.OpQueryExist)
	switch _, err := elq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (elq *EngineLogQuery) ExistX(ctx context.Context) bool {
	exist, err := elq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EngineLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (elq *EngineLogQuery) Clone() *EngineLogQuery {
	if elq == nil {
		return nil
	}
	return &EngineLogQuery{
		config:     elq.config,
		ctx:        elq.ctx.Clone(),
		order:      append([]enginelog.OrderOption{}, elq.order...),
		inters:     append([]Interceptor{}, elq.inters...),
		predicates: append([]predicate.EngineLog{}, elq.predicates...),
		// clone intermediate query.
		sql:       elq.sql.Clone(),
		path:      elq.path,
		modifiers: append([]func(*sql.Selector){}, elq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ModelingID int `json:"modeling_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EngineLog.Query().
//		GroupBy(enginelog.FieldModelingID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (elq *EngineLogQuery) GroupBy(field string, fields ...string) *EngineLogGroupBy {
	elq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EngineLogGroupBy{build: elq}
	grbuild.flds = &elq.ctx.Fields
	grbuild.label = enginelog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ModelingID int `json:"modeling_id,omitempty"`
//	}
//
//	client.EngineLog.Query().
//		Select(enginelog.FieldModelingID).
//		Scan(ctx, &v)
func (elq *EngineLogQuery) Select(fields ...string) *EngineLogSelect {
	elq.ctx.Fields = append(elq.ctx.Fields, fields...)
	sbuild := &EngineLogSelect{EngineLogQuery: elq}
	sbuild.label = enginelog.Label
	sbuild.flds, sbuild.scan = &elq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EngineLogSelect configured with the given aggregations.
func (elq *EngineLogQuery) Aggregate(fns ...AggregateFunc) *EngineLogSelect {
	return elq.Select().Aggregate(fns...)
}

func (elq *EngineLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range elq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, elq); err != nil {
				return err
			}
		}
	}
	for _, f := range elq.ctx.Fields {
		if !enginelog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if elq.path != nil {
		prev, err := elq.path(ctx)
		if err != nil {
			return err
		}
		elq.sql = prev
	}
	return nil
}

func (elq *EngineLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EngineLog, error) {
	var (
		nodes = []*EngineLog{}
		_spec = elq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EngineLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EngineLog{config: elq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(elq.modifiers) > 0 {
		_spec.Modifiers = elq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, elq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (elq *EngineLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := elq.querySpec()
	if len(elq.modifiers) > 0 {
		_spec.Modifiers = elq.modifiers
	}
	_spec.Node.Columns = elq.ctx.Fields
	if len(elq.ctx.Fields) > 0 {
		_spec.Unique = elq.ctx.Unique != nil && *elq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, elq.driver, _spec)
}

func (elq *EngineLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(enginelog.Table, enginelog.Columns, sqlgraph.NewFieldSpec(enginelog.FieldID, field.TypeInt))
	_spec.From = elq.sql
	if unique := elq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if elq.path != nil {
		_spec.Unique = true
	}
	if fields := elq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, enginelog.FieldID)
		for i := range fields {
			if fields[i] != enginelog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := elq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := elq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := elq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := elq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (elq *EngineLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(elq.driver.Dialect())
	t1 := builder.Table(enginelog.Table)
	columns := elq.ctx.Fields
	if len(columns) == 0 {
		columns = enginelog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if elq.sql != nil {
		selector = elq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if elq.ctx.Unique != nil && *elq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range elq.modifiers {
		m(selector)
	}
	for _, p := range elq.predicates {
		p(selector)
	}
	for _, p := range elq.order {
		p(selector)
	}
	if offset := elq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := elq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (elq *EngineLogQuery) Modify(modifiers ...func(s *sql.Selector)) *EngineLogSelect {
	elq.modifiers = append(elq.modifiers, modifiers...)
	return elq.Select()
}

// EngineLogGroupBy is the group-by builder for EngineLog entities.
type EngineLogGroupBy struct {
	selector
	build *EngineLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (elgb *EngineLogGroupBy) Aggregate(fns ...AggregateFunc) *EngineLogGroupBy {
	elgb.fns = append(elgb.fns, fns...)
	return elgb
}

// Scan applies the selector query and scans the result into the given value.
func (elgb *EngineLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, elgb.build.ctx, ent.OpQueryGroupBy)
	if err := elgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EngineLogQuery, *EngineLogGroupBy](ctx, elgb.build, elgb, elgb.build.inters, v)
}

func (elgb *EngineLogGroupBy) sqlScan(ctx context.Context, root *EngineLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(elgb.fns))
	for _, fn := range elgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*elgb.flds)+len(elgb.fns))
		for _, f := range *elgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*elgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := elgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EngineLogSelect is the builder for selecting fields of EngineLog entities.
type EngineLogSelect struct {
	*EngineLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (els *EngineLogSelect) Aggregate(fns ...AggregateFunc) *EngineLogSelect {
	els.fns = append(els.fns, fns...)
	return els
}

// Scan applies the selector query and scans the result into the given value.
func (els *EngineLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, els.ctx, ent.OpQuerySelect)
	if err := els.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EngineLogQuery, *EngineLogSelect](ctx, els.EngineLogQuery, els, els.inters, v)
}

func (els *EngineLogSelect) sqlScan(ctx context.Context, root *EngineLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(els.fns))
	for _, fn := range els.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*els.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := els.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (els *EngineLogSelect) Modify(modifiers ...func(s *sql.Selector)) *EngineLogSelect {
	els.modifiers = append(els.modifiers, modifiers...)
	return els
}
