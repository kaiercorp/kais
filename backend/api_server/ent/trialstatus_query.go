// Code generated by ent, DO NOT EDIT.

package ent

import (
	"api_server/ent/predicate"
	"api_server/ent/trialstatus"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TrialStatusQuery is the builder for querying TrialStatus entities.
type TrialStatusQuery struct {
	config
	ctx        *QueryContext
	order      []trialstatus.OrderOption
	inters     []Interceptor
	predicates []predicate.TrialStatus
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TrialStatusQuery builder.
func (tsq *TrialStatusQuery) Where(ps ...predicate.TrialStatus) *TrialStatusQuery {
	tsq.predicates = append(tsq.predicates, ps...)
	return tsq
}

// Limit the number of records to be returned by this query.
func (tsq *TrialStatusQuery) Limit(limit int) *TrialStatusQuery {
	tsq.ctx.Limit = &limit
	return tsq
}

// Offset to start from.
func (tsq *TrialStatusQuery) Offset(offset int) *TrialStatusQuery {
	tsq.ctx.Offset = &offset
	return tsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tsq *TrialStatusQuery) Unique(unique bool) *TrialStatusQuery {
	tsq.ctx.Unique = &unique
	return tsq
}

// Order specifies how the records should be ordered.
func (tsq *TrialStatusQuery) Order(o ...trialstatus.OrderOption) *TrialStatusQuery {
	tsq.order = append(tsq.order, o...)
	return tsq
}

// First returns the first TrialStatus entity from the query.
// Returns a *NotFoundError when no TrialStatus was found.
func (tsq *TrialStatusQuery) First(ctx context.Context) (*TrialStatus, error) {
	nodes, err := tsq.Limit(1).All(setContextOp(ctx, tsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{trialstatus.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tsq *TrialStatusQuery) FirstX(ctx context.Context) *TrialStatus {
	node, err := tsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TrialStatus ID from the query.
// Returns a *NotFoundError when no TrialStatus ID was found.
func (tsq *TrialStatusQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tsq.Limit(1).IDs(setContextOp(ctx, tsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{trialstatus.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tsq *TrialStatusQuery) FirstIDX(ctx context.Context) int {
	id, err := tsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TrialStatus entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TrialStatus entity is found.
// Returns a *NotFoundError when no TrialStatus entities are found.
func (tsq *TrialStatusQuery) Only(ctx context.Context) (*TrialStatus, error) {
	nodes, err := tsq.Limit(2).All(setContextOp(ctx, tsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{trialstatus.Label}
	default:
		return nil, &NotSingularError{trialstatus.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tsq *TrialStatusQuery) OnlyX(ctx context.Context) *TrialStatus {
	node, err := tsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TrialStatus ID in the query.
// Returns a *NotSingularError when more than one TrialStatus ID is found.
// Returns a *NotFoundError when no entities are found.
func (tsq *TrialStatusQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tsq.Limit(2).IDs(setContextOp(ctx, tsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{trialstatus.Label}
	default:
		err = &NotSingularError{trialstatus.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tsq *TrialStatusQuery) OnlyIDX(ctx context.Context) int {
	id, err := tsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TrialStatusSlice.
func (tsq *TrialStatusQuery) All(ctx context.Context) ([]*TrialStatus, error) {
	ctx = setContextOp(ctx, tsq.ctx, "All")
	if err := tsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TrialStatus, *TrialStatusQuery]()
	return withInterceptors[[]*TrialStatus](ctx, tsq, qr, tsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tsq *TrialStatusQuery) AllX(ctx context.Context) []*TrialStatus {
	nodes, err := tsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TrialStatus IDs.
func (tsq *TrialStatusQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tsq.ctx.Unique == nil && tsq.path != nil {
		tsq.Unique(true)
	}
	ctx = setContextOp(ctx, tsq.ctx, "IDs")
	if err = tsq.Select(trialstatus.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tsq *TrialStatusQuery) IDsX(ctx context.Context) []int {
	ids, err := tsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tsq *TrialStatusQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tsq.ctx, "Count")
	if err := tsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tsq, querierCount[*TrialStatusQuery](), tsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tsq *TrialStatusQuery) CountX(ctx context.Context) int {
	count, err := tsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tsq *TrialStatusQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tsq.ctx, "Exist")
	switch _, err := tsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tsq *TrialStatusQuery) ExistX(ctx context.Context) bool {
	exist, err := tsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TrialStatusQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tsq *TrialStatusQuery) Clone() *TrialStatusQuery {
	if tsq == nil {
		return nil
	}
	return &TrialStatusQuery{
		config:     tsq.config,
		ctx:        tsq.ctx.Clone(),
		order:      append([]trialstatus.OrderOption{}, tsq.order...),
		inters:     append([]Interceptor{}, tsq.inters...),
		predicates: append([]predicate.TrialStatus{}, tsq.predicates...),
		// clone intermediate query.
		sql:  tsq.sql.Clone(),
		path: tsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TrialUUID string `json:"trial_uuid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TrialStatus.Query().
//		GroupBy(trialstatus.FieldTrialUUID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tsq *TrialStatusQuery) GroupBy(field string, fields ...string) *TrialStatusGroupBy {
	tsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TrialStatusGroupBy{build: tsq}
	grbuild.flds = &tsq.ctx.Fields
	grbuild.label = trialstatus.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TrialUUID string `json:"trial_uuid,omitempty"`
//	}
//
//	client.TrialStatus.Query().
//		Select(trialstatus.FieldTrialUUID).
//		Scan(ctx, &v)
func (tsq *TrialStatusQuery) Select(fields ...string) *TrialStatusSelect {
	tsq.ctx.Fields = append(tsq.ctx.Fields, fields...)
	sbuild := &TrialStatusSelect{TrialStatusQuery: tsq}
	sbuild.label = trialstatus.Label
	sbuild.flds, sbuild.scan = &tsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TrialStatusSelect configured with the given aggregations.
func (tsq *TrialStatusQuery) Aggregate(fns ...AggregateFunc) *TrialStatusSelect {
	return tsq.Select().Aggregate(fns...)
}

func (tsq *TrialStatusQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tsq); err != nil {
				return err
			}
		}
	}
	for _, f := range tsq.ctx.Fields {
		if !trialstatus.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tsq.path != nil {
		prev, err := tsq.path(ctx)
		if err != nil {
			return err
		}
		tsq.sql = prev
	}
	return nil
}

func (tsq *TrialStatusQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TrialStatus, error) {
	var (
		nodes = []*TrialStatus{}
		_spec = tsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TrialStatus).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TrialStatus{config: tsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(tsq.modifiers) > 0 {
		_spec.Modifiers = tsq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tsq *TrialStatusQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tsq.querySpec()
	if len(tsq.modifiers) > 0 {
		_spec.Modifiers = tsq.modifiers
	}
	_spec.Node.Columns = tsq.ctx.Fields
	if len(tsq.ctx.Fields) > 0 {
		_spec.Unique = tsq.ctx.Unique != nil && *tsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tsq.driver, _spec)
}

func (tsq *TrialStatusQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(trialstatus.Table, trialstatus.Columns, sqlgraph.NewFieldSpec(trialstatus.FieldID, field.TypeInt))
	_spec.From = tsq.sql
	if unique := tsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tsq.path != nil {
		_spec.Unique = true
	}
	if fields := tsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, trialstatus.FieldID)
		for i := range fields {
			if fields[i] != trialstatus.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tsq *TrialStatusQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tsq.driver.Dialect())
	t1 := builder.Table(trialstatus.Table)
	columns := tsq.ctx.Fields
	if len(columns) == 0 {
		columns = trialstatus.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tsq.sql != nil {
		selector = tsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tsq.ctx.Unique != nil && *tsq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range tsq.modifiers {
		m(selector)
	}
	for _, p := range tsq.predicates {
		p(selector)
	}
	for _, p := range tsq.order {
		p(selector)
	}
	if offset := tsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tsq *TrialStatusQuery) Modify(modifiers ...func(s *sql.Selector)) *TrialStatusSelect {
	tsq.modifiers = append(tsq.modifiers, modifiers...)
	return tsq.Select()
}

// TrialStatusGroupBy is the group-by builder for TrialStatus entities.
type TrialStatusGroupBy struct {
	selector
	build *TrialStatusQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tsgb *TrialStatusGroupBy) Aggregate(fns ...AggregateFunc) *TrialStatusGroupBy {
	tsgb.fns = append(tsgb.fns, fns...)
	return tsgb
}

// Scan applies the selector query and scans the result into the given value.
func (tsgb *TrialStatusGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tsgb.build.ctx, "GroupBy")
	if err := tsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TrialStatusQuery, *TrialStatusGroupBy](ctx, tsgb.build, tsgb, tsgb.build.inters, v)
}

func (tsgb *TrialStatusGroupBy) sqlScan(ctx context.Context, root *TrialStatusQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tsgb.fns))
	for _, fn := range tsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tsgb.flds)+len(tsgb.fns))
		for _, f := range *tsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TrialStatusSelect is the builder for selecting fields of TrialStatus entities.
type TrialStatusSelect struct {
	*TrialStatusQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tss *TrialStatusSelect) Aggregate(fns ...AggregateFunc) *TrialStatusSelect {
	tss.fns = append(tss.fns, fns...)
	return tss
}

// Scan applies the selector query and scans the result into the given value.
func (tss *TrialStatusSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tss.ctx, "Select")
	if err := tss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TrialStatusQuery, *TrialStatusSelect](ctx, tss.TrialStatusQuery, tss, tss.inters, v)
}

func (tss *TrialStatusSelect) sqlScan(ctx context.Context, root *TrialStatusQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tss.fns))
	for _, fn := range tss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tss *TrialStatusSelect) Modify(modifiers ...func(s *sql.Selector)) *TrialStatusSelect {
	tss.modifiers = append(tss.modifiers, modifiers...)
	return tss
}
